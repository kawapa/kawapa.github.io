---
layout: post
title: Git czyli system kontroli wersji
date: 2021-11-28 17:00:00
permalink: /XXX
search_exclude: true
---

Sciagawka

https://github.com/arslanbilal/git-cheat-sheet/blob/master/other-sheets/git-cheat-sheet-pl.md





Git
Co zrobi komenda commit bez -m?
Przesuniecie ze stage to working directory
git restore --staged PLIK
git add -u
Dodaje do poczekalni tylko pliki, które były śledzone
Git
### Usunięcie taga (local)
git tag -d <tag_name>
ZMIANA NAZWY BRANCHA
https://training.github.com/downloads/pl/github-git-cheat-sheet/
Wycofująć (revert) merge'a, nie da się go później ponownie zrobić
 
 
 
 
Remote
Cherry pick
git cherry-pick -x <commit-hash>
git amend
Poprawka nazwy ostatniego LOKALNEGO commita
git commit --amend -m "an updated commit message"
Poprawka ostatniego LOKALNEGO commita
Wprowadzamy zmiany
Git add <plik1> <plik2>
git commit --amend --no-edit
git tag
https://git-scm.com/book/en/v2/Git-Basics-Tagging
Tworzenie taga
git tag <NAZWA>
git tag <NAZWA> <SHA>
Tworzenie taga opisanego
git tag -a <NAZWA> -m <KOMENTARZ>
git stash
git stash list [<options>]
git stash show [<options>] [<stash>]
git stash drop [-q|--quiet] [<stash>]
git stash ( pop | apply ) [--index] [-q|--quiet] [<stash>]
git stash branch <branchname> [<stash>]
git stash [push [-p|--patch] [-k|--[no-]keep-index] [-q|--quiet]
git stash clear
git stash create [<message>]
git stash store [-m|--message <message>] [-q|--quiet] <commit>
https://blogprogramisty.net/komendy-git-a-ktore-nalezy-znac/
https://blogprogramisty.net/komendy-git-a-ktorych-nie-moge-zapamietac/
https://stackoverflow.com/questions/5308816/how-can-i-merge-multiple-commits-onto-another-branch-as-a-single-squashed-commit
https://www.internalpointers.com/post/squash-commits-into-one-git
























git init - tworzy repozytorium od zera

git clone <adres> - klonowanie 



`origin` - podstawowy serwer zdalny




`git checkout -- <plik>` - CO TO ROBI

`git restore --staged <file>` - wycofanie pliku z poczekalni


### 1. Konfiguracja gita

`git config --global -l` - konfiguracja globalna
`git config --local -l` - konfiguracja loklana (odnosi się do repozytorium, w którym jesteśmy)



`git commit` (bez argumentu -m) - otworzy się domyślny edytor tekstu (`nano`/`vim`) i będize trzeba podac i tak opis


## Wyswietlanie zmian

git show - wyświetla zmiany dokonane w najnowszym commicie.
git show SHA - wyświetla zmiany dokonane w commicie o numerze SHA

## 1. Poprawki do ostatniego commit-a

**Nie wolno używać w sytuacji, że nad repozytorium pracuje ktoś jeszcze oprócz nas!**

* Wprowadzamy zmiany w plikach
* `git add .`
* `git commit --amend --no-edit`

## 2. Porównanie katalogu roboczego z ostatnia rewizja

`git diff`
`git diff file` - wyświetla zmiany, które nie zostały jeszcze wprowadzone do indexu w pliku o nazwie file

## 3. Różnica między poczekalnią, a ostatnią rewizją

`git diff --staged`<br>
`git diff --cached`

## 4. Przywracanie zmian

### 4.1 `git checkout`

Operacja polegająca na przesunięciu wskaźnika HEAD na wskazany commit.

**Przywracanie pojedynczego pliku**

`git checkout <COMMIT> <PLIK>`

**Przywracanie stanu repozytorium**

`git checkout <COMMIT>`

W ten sposób trafiamy do trybu tzw. odłączonej głowy (`detached HEAD`). Można przeglądać projekt i wprowadzać zmiany. W celu zapisana zmian tworzymy nową gałąź:

`git checkout -b <NOWY_BRANCH>`

### 4.2 git revert

Operacja polegająca na **odwróceniu zmian** z wybranego commit-u i zapisaniu ich jako nowy commit.

`git revert <COMMIT>`

**Wybieramy commit który chcemy wycofać, a nie ten do którego chcemy się cofnąć!**

`git revert HEAD~3` - (cofa o 3 rewizje do tyłu)

### 4.3 git reset

Przywraca zmiany w repozytorium do wskazanego punktu w historii.

**Uwaga! To polecenie modyfikuje historię Git'a!**
**W związku z tym powinno się je stosować tylko do commit-ów, które nie zostały opublikowane komendą `git push`.**

`git reset --mixed <COMMIT>`<br>
> `--mixed` - domyślne (nie trzeba pisać), wszystkie zmiany wprowadzone po commicie do którego się cofamy zostaną w katalogu roboczym<br>
> `--soft` - wszystkie zmiany (...) zostaną przeniesione na `stage`<br>
> `--hard` - wszystkie zmiany (...) zostaną usunięte (!)

## 5. Czyszczenie katalogu roboczego z niechcianych plikow

Polecenie służące do usuwania z katalogu roboczego nieśledzonych plików i katalogów.

`git clean -ndf`
> `-n` - wyświetla listę plików<br>
> `-nd` - wyświetla listę plików i katalogów do usunięcia<br>
> `-i` - tryb interaktywny (pyta o każdy plik/katalog)<br>
> `-f` - usuwa bez oczekiwania na potwierdzenie

**Przykłady**

`git clean -ndf` - usuwa wszystkie pliki i katalogi
`git clean -f` - usuwa wszystkie pliki

## 6. Wprowadzanie zmian z konkretnego commit-a

Przydatne jeśli chcemy wprowadzić zmiany z konkretnego commit-a znajdującego się na przykład na innym branchu. Nie chcemy robić `merge` ani `rebase` bo to wprowadzi zmiany ze wszystkich commit-ów na danym branchu.

`git cherry-pick <COMMIT_1> <COMMIT_2>` - to stworzy nowy commit na aktywnym branchu

`git cherry-pick <COMMIT_1> -n` - to naniesie zmiany na przestrzeń roboczą ale nie stworzy commit-a










// delete branch locally
git branch -d localBranchName

// delete branch remotely
git push origin --delete remoteBranchName



// pokazuje wszystkie remoty
git remove -v

// dodanie remote
git remote add NAZWA-REMOTE'a <adres-repo>






| Komenda | Opis |
|-------|--------|
| `git stash list` | Listuje wszystkie zachowane stash'e |
| | |
| `git stash pop` | XXX |
| `git stash pop stash@{5} | |
| | |
| `git stash apply` | |
| `git stash apply stash@{5} | |


* ostatnio wykonany stash to zawsze `stash{0}`



`git stash list` - Listuje wszystkie zarchiwizowane stashe


---
`git stash list`

{% highlight sh %}
stash@{0}: WIP on master: 1c8da66 Add the author name
{% endhighlight %}

---

`git stash show` *

{% highlight sh %}
stash@{0}: WIP on master: 1c8da66 Add the author name
{% endhighlight %}

`git stash show` *

{% highlight sh %}
 README.md | 1 +
 1 file changed, 1 insertion(+)
{% endhighlight %}

`git stash show -p`

s
 
 ## Second
 
+asd
\ No newline at end of file

## Bibliografia

* Dokumentacja Git'a - `git stash --help`




## Bibliografia

* http://home.agh.edu.pl/~glowacki/docs/matwykl/O-o/_ProgObiekt-C++14-Rozsz.pdf
